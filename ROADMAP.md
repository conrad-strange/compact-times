# 设计依据与路线图

更新日期：2026-09-17

## 定位

提供一个可在 Codex 对话中调用的 `times` 技能，按需输出该对话在本地日志中记录的压缩次数。实现由 `SKILL.md` 和 Python 标准库脚本组成，安装及使用见 [README.md](README.md)。

当前已经完成脚本、个人技能安装验证、本地测试、GitHub 发布和远端 CI 验证。没有为这个单一查询增加后台服务、MCP、hooks 或额外界面。

## 必要性复核

2026-09-17 查阅了官方文档、可搜索插件目录和相关开源项目。未找到已确认可直接完成“当前 Codex 对话的一次性压缩次数查询”的现成方案；这不代表所有第三方项目都没有此功能。

| 已有方案 | 本次核查结果 |
| --- | --- |
| 官方 `/status` | 文档列出了对话 ID、上下文用量和限额，未列出累计压缩次数 |
| 官方 `/compact` | 执行压缩，而非查询历史次数 |
| 官方 `/usage`（CLI） | 账户 token 活动查询 |
| 插件目录 | 搜索 `compaction` 为空；组合关键词搜索未找到对应计数能力 |
| `compact-plus` | 保存和恢复压缩前后的工作状态，项目说明未提供本需求的计数入口 |
| `ccusage` | 提供 Codex 日志的 token、成本及会话统计，文档未列出压缩次数指标 |

来源：[桌面命令](https://learn.chatgpt.com/docs/reference/slash-commands)、[CLI 命令](https://learn.chatgpt.com/docs/developer-commands?surface=cli)、[compact-plus](https://github.com/u-ichi/compact-plus)、[ccusage](https://ccusage.com/guide/codex/)。相关第三方项目只检查了公开说明，没有安装评测。

结论是为重复查询做一个小技能有价值。以后若官方提供等价的直接计数功能，应优先复用官方能力。

## 入口选择

官方文档支持用 `$` 选择技能，并说明启用的技能会出现在桌面端斜杠命令列表。因此采用 `$times` 或在 `/` 菜单筛选 `times` 的形式。[官方技能说明](https://learn.chatgpt.com/docs/build-skills)

当前 Codex 已能发现安装的 `times` 技能，实际安装脚本也已验证；桌面输入框中直接键入 `/times` 后回车的行为没有做 UI 自动化验证。

本次没有找到普通技能或插件为内置 `/status` 追加字段的公开接口，因此没有修改客户端。技能调用可能消耗一次模型交互；统计脚本自身不调用模型 API。

## 实现与边界

1. 从显式参数或 `CODEX_THREAD_ID` 获取目标 UUID。
2. 按 ID 定位活动或归档目录中的目标日志，校验会话身份。
3. 在固定文件长度快照内，流式读取顶层 `compacted` 记录。
4. 有事件标识时去重；输出一行结果或无法统计的原因。

已验证的普通日志包含独立的 `compaction_response_id`、`window_id` 和递增窗口编号。窗口编号只用于人工核对，不参与计数公式。

对于父任务、fork 信息、多段会话元数据、多个同 ID 日志、未知格式及坏行，当前选择返回明确错误。该限制保证简单实现不会把有歧义的继承记录直接当作该对话的确定次数。

统计范围始终是本地保留记录，不能恢复已删除或未同步的历史。旧格式无事件标识时按记录数统计，不能识别由外部工具复制的重复记录。

## 完成情况

- [x] 官方能力及相关方案复核。
- [x] 按需读取脚本，无第三方运行依赖。
- [x] 当前任务识别、显式指定任务及归档定位。
- [x] 计数、去重、错误处理和命令行集成测试。
- [x] 个人技能安装及 Codex 技能发现验证。
- [x] 真实长对话只读验证，5 次到 6 次的变化与新增记录一致。
- [x] 通用 README、conda 环境说明、MIT 许可证、Git 忽略规则和 CI 配置。
- [x] 发布到 [GitHub](https://github.com/conrad-strange/compact-times)，并通过远端 Windows / Ubuntu CI。
- [ ] 收集不同 Codex 版本的最小合成日志样本，按实际需求增加兼容支持。

只有出现明确的分发需求时，才增加插件 manifest。持续监听、标题栏数字、时间线和综合 token 分析不在当前范围内。
